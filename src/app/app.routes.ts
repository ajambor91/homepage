import { Routes } from '@angular/router';
import {IntrusionComponent} from "./components/intrusion/intrusion.component";

export const routes: Routes = [
  {path: '', component: IntrusionComponent}
];
